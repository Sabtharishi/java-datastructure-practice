package com.rishi.ds.stack;

/**
 * Stack is abstract data type which demonstrates Last in first out (LIFO)
 * behavior This example implements same behavior using Linked List.
 * 
 * There are two most important operations of Stack:
 * <li>Push : We will push element to beginning of linked list to demonstrate
 * push behavior of stack.</li>
 * <li>Pop : We will remove first element of linked list to demonstrate pop
 * behavior of Stack .</li>
 * 
 * @author Rishi
 *
 */
public class StackUsingLinkedList {

	private int size;
	public void setSize(){
		this.size = 1;
	}
}
